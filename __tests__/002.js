import {
  getNames,
  getValues,
  getSortedUniqueValues,
  getPropsB,
  getIndexedObject,
  getNamesFromObjects
} from "../questions/002";
import { isEqualArray } from "../_utils/array";

describe("Question 002", () => {
  describe('getNames', () => {
    it('basic', () => {});
  })
});
