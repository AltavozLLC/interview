/*
  Given an array of strings, an item, and a boolean, add or remove the item from the array based on the boolean
  If checked is true then you will want to add the item to the array only if it doesn't already exist
  If checked is false then you will want to remove the item from the array

  Example1:                          Example 2:                           Example 3:
    INPUT                              INPUT                                INPUT
      list = ['a', 'b', 'c']             list = ['a', 'b', 'c']               list = ['a', 'b', 'c']
      item = 'd'                         item = 'c'                           item = 'c'
      checked = true                     checked = true                       checked = false
    OUTPUT                             OUTPUT                               OUTPUT
      ['a', 'b', 'c', 'd']               ['a', 'b', 'c']                      ['a', 'b']
*/

function updateItemInArray(
  list,     // string[]
  id,       // string
  checked   // boolean
) {
  // Write code here
}

export default updateItemInArray